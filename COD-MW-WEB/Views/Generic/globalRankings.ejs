<!DOCTYPE html>
<html lang="it-IT">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/Public/style.css">
    <title>Classifica Globale</title>
</head>
<body>
    <div class="form-insert">
        <form action="/globalRankings" method="post">
            <% if(!rankings) {%> 
                <a href="/home">Torna indietro</a>
    
                <label for="tournamentDate">Data Torneo</label>
                <input type="date" name="tournamentDate" required>
        
                <input type="submit" value="Vedi classifica">
            <% } else { %>
                <div class="table-management">
                    <table>
                        <tr>
                            <th>ID Torneo</th>
                            <th>Data inizio</th>
                            <th>Ora inizio</th>
                            <th>Ora fine</th>
                        </tr>
                        <tr>
                            <% 
                                var date = new Date(rankings.start_date.utcStartSeconds * 1000).toLocaleDateString('it-IT'); 
                            %>
                            <td><%= rankings.id_tournament %></td>
                            <td><%= rankings.number_matches %></td>
                            <td><%= %></td>
                            <td><%= new Date(rankings.start_time.utcStartSeconds * 1000).toLocaleTimeString('it-IT'); %></td>
                            <td><%= new Date(rankings.end_time.utcStartSeconds * 1000).toLocaleTimeString('it-IT'); %></td>
                        </tr>
                    </table>
                    
                </div>
                <a class="button-link" href="/globalRankings">Torna indietro</a>
            <% } %>
            <a class="button-link" href="/home">Torna indietro</a>
        </form>
    </div>
    
</body>
</html>