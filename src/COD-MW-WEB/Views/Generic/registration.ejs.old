<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/Public/style.css">
    <!-- google fonts -->
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&display=swap" rel="stylesheet">
    <title>Registrazione</title>
</head>
<body>
    <div class="form-insert">
        <h1>Registrazione</h1>
        <form action="/registration" method="post" autocomplete="off">
            <% if(registrationError === 'generic') { %>
                <p style="color: red;">Errore generico, riprova di nuovo!</p>
            <% } else %>
            <% if(registrationError === 'email') { %>
                <p style="color: red;">Errore Email</p>
            <% } else %>
            <% if(registrationError === 'password_cod') { %>
                <p style="color: red;">Errore password cod</p>
            <% } else %>
            <% if(registrationError === 'password') { %>
                <p style="color: red;">Errore password</p>
            <% } else%>
            <% if(registrationError === 'failed') { %>
                <p style="color: red;">Test login nel server di COD FALLITA! Controlla di aver inserito le credenziali correttamente</p>
            <% } else %>
            <% if(registrationError === 'exists_email') { %>
                <p style="color: red;">Esiste già questa email!</p>
            <% }%>
            <input type="email" name="email_cod" placeholder="Email di Call Of Duty">
            <input type="password" name="password_cod" placeholder="Password di Call Of Duty">
            <span>Se non hai un account di Call Of Duty premi <a target="_blank" href="https://profile.callofduty.com/cod/signup">qui</a></span>
            <input type="checkbox" name="agree">Sono d'accordo che potete accedere il mio account
            <br><br>
            Piattaforma che giochi ora:
            <select name="platform">
                <option value="psn" selected>PlayStation</option>
                <option value="battle" selected>battle.net</option>
                <option value="xbl" selected>xbox</option>
            </select>
            <br><br>
            <span>Inserisci una password per poter accedere al nostro sito!</span>
            <span></span>
            <input type="password" name="password" placeholder="Password">
            <input type="submit" value="Conferma">
            <a class="button-link" href="/login">Torna indietro</a>
    </div>
</body>
</html>